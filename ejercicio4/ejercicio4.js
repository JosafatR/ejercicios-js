// Solución ejercicio 4.
console.log("Ejercicio 4");

const student1Courses = ['Math', 'English', 'Programming'];
const student2Courses = ['Geography', 'Spanish', 'Programming'];

const result = student1Courses.filter((student) => student.length > 4);
const result = student2Courses.filter((student) => student.length > 4);

console.log(student1Courses.includes (result) );
console.log(student2Courses.includes (result) );


